# создание директолрии python -m venv my_venv
# удаление директории  rm myenv/
# удаление рекурсивно rm -r my_env
# cat my_env/bin/activate
# head 
a = 1

if a > 2:
    print(f"num {a} is grether than 3")
else:
    print(f"num {a} is less than 2")
